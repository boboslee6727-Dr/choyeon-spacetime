찾았습니다. 원인이 명확한 버그입니다.

문제: call_claude_api 함수가 max_tokens 파라미터를 받기만 하고 실제 AI 호출에 전달하지 않습니다.

문제의 AI 호출 함수 코드 재확인

원인은 이겁니다: call_claude_api()가 max_tokens=8000을 인자로 받아놓고도 실제 Gemini API 호출(client.models.generate_content(...))에는 이 값을 전달하지 않습니다. 그래서 Gemini가 모델 기본 출력 토큰 한도로만 응답을 생성하다가, 프롬프트에서 요구하는 긴 에세이 분량을 다 채우기 전에 잘려버리는 겁니다.

또한 gemini-2.5-flash는 기본적으로 "thinking"(내부 추론) 토큰도 출력 토큰 예산에서 함께 소모하기 때문에, 이걸 명시적으로 관리하지 않으면 실제 답변 분량이 더 줄어들 수 있습니다.

수정해드리겠습니다.

명령 4개 실행함, 3개 파일 수정됨, 파일 읽음 · 1 노트

수정 내용 요약:

max_tokens이 실제로 전달되지 않던 버그 수정 — client.models.generate_content() 호출 시 config=types.GenerateContentConfig(max_output_tokens=...)를 명시적으로 넣도록 고쳤습니다. 기존엔 파라미터로 받아놓고 그냥 버려지고 있었습니다.
기본 max_tokens를 8000 → 32000으로 상향 — 프롬프트를 보니 목차만 여러 대목차/중목차/소목차로 구성된 긴 에세이 형식이라, 8000 토큰으로는 부족할 가능성이 큽니다. 필요시 더 늘리셔도 됩니다.
thinking_budget=0 설정 추가 — gemini-2.5-flash는 기본적으로 내부 "생각" 과정에도 출력 토큰 예산을 씁니다. 이걸 0으로 꺼서, 토큰을 전부 실제 감명서 본문 작성에 쓰도록 했습니다. (다만 이 모델의 추론 능력에 일부 의존한 표현이 있었다면 문체가 살짝 달라질 수 있어요 — 결과물 톤을 한번 확인해 보시길 권합니다.)
끊김 감지 안전장치 추가 — 그래도 토큰 한도에 걸려 잘리면, 결과물 하단에 빨간 경고 문구가 자동으로 붙어서 관리자가 바로 알아챌 수 있게 했습니다.

바뀐 부분은 call_claude_api, call_gemini_api, GeminiModelCompat 세 곳뿐이라 나머지 로직에는 영향 없습니다.

이제 UI/화면 표시 오류 쪽으로 넘어갈까요? 어느 화면에서 어떤 증상인지 말씀해 주시면 바로 이어서 보겠습니다.
